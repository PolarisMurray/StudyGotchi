/**
 * Live2D Widget Library
 * 
 * NOTE: This is a placeholder file. 
 * You need to either:
 * 1. Build it from the live2d-widget source: https://github.com/stevenjoezhang/live2d-widget
 * 2. Download the compiled version from the repository
 * 3. Use a CDN version if available
 * 
 * The live2d-widget project needs to be built using:
 * npm install
 * npm run build
 * 
 * Then copy the dist/live2d.min.js file to this location.
 */

console.warn('Live2D library not loaded. Please build or download live2d.min.js from https://github.com/stevenjoezhang/live2d-widget');

// Placeholder function to prevent errors
if (typeof window.loadlive2d === 'undefined') {
  window.loadlive2d = function(elementId, config) {
    console.warn('Live2D library not available. Please install live2d.min.js');
    const element = document.getElementById(elementId);
    if (element) {
      element.innerHTML = '<div style="display: flex; align-items: center; justify-content: center; height: 100%; color: #999;">Live2D model will appear here</div>';
    }
  };
}

